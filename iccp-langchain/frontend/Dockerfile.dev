FROM node:20-alpine

WORKDIR /app
COPY package*.json ./
RUN npm ci

COPY . .

# 开发容器默认指向本机后端，可在 docker run -e 覆盖
ENV VITE_API_BASE_URL=http://host.docker.internal:8000

EXPOSE 3000
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]
